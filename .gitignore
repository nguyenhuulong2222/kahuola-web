node_modules/
.wrangler/
worker/node_modules/
worker/.wrangler/
.claude/
_merge_work

# macOS metadata — never belongs in the repo
.DS_Store

# local tool output & session archives (rebuilt/kept locally, never in repo)
.agents/
.codex/
graphify-out/
docs/seo-session-archive-2026-05/

# morning-brief runtime output (regenerated each run)
scripts/insights/brief-latest.json

# secrets — never commit (tokens live in ~/.kahuola-secrets, outside the repo)
.kahuola-secrets/
*.token
*cf-analytics-token*
*cf-account-id*
CF_ANALYTICS_TOKEN*
# any n8n export whose name advertises a token — one `git add -A` away from
# being published otherwise
*WITH-TOKEN*

# Untracked 2026-09-24. Cloudflare Pages serves the repo root verbatim, so a
# tracked file is a published file. These are build junk, scratch copies and a
# local AI workspace — none of them site assets. They stay on disk.
tmp_compare/
_to_wire_into_worker/
KahuOla_AI_Ready for Gemma 4 workspae/
*.zip
