node_modules/
.wrangler/
worker/node_modules/
worker/.wrangler/
.claude/
_merge_work

# macOS metadata — never belongs in the repo
.DS_Store

# graphify tool output (knowledge-graph build artifact, rebuilt locally)
graphify-out/SEO_*.md # Or specific filenames

# morning-brief runtime output (regenerated each run)
scripts/insights/brief-latest.json

# secrets — never commit (tokens live in ~/.kahuola-secrets, outside the repo)
.kahuola-secrets/
*.token
*cf-analytics-token*
*cf-account-id*
CF_ANALYTICS_TOKEN*
